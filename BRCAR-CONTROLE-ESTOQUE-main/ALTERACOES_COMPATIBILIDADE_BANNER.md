# Correções adicionais

- Banner do catálogo: o campo `catalog_banner_url` agora é retornado no site público e a imagem é renderizada no topo do catálogo.
- Subtítulo do catálogo: passou a usar o valor salvo nas configurações.
- Compatibilidade visual: as cores modernas em `oklch()` e `color-mix()` foram convertidas para sRGB/rgba, reduzindo diferenças de aparência em navegadores/Windows mais antigos.
- Foram preservadas as correções anteriores de cadastro (`team-signup`) e otimização de imagens.
